{
  "name": "vrom.js",
  "version": "3.4.0",
  "description": "Zero-boilerplate RAG for browser AI agents. WASM HNSW vector search + background ONNX embedding + OPFS-cached vROM cartridges.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    },
    "./embed-worker": {
      "import": {
        "types": "./dist/embed-worker.d.ts",
        "default": "./dist/embed-worker.js"
      },
      "require": {
        "types": "./dist/embed-worker.d.ts",
        "default": "./dist/embed-worker.js"
      }
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "wasm-pkg/*.json",
    "wasm-pkg/*.js",
    "wasm-pkg/*.wasm",
    "wasm-pkg/*.d.ts",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build:wasm": "wasm-pack build ../src --target bundler --out-dir ../lib/wasm-pkg --release && rm -f ../lib/wasm-pkg/.gitignore",
    "build:js": "tsdown",
    "build": "npm run build:wasm && npm run build:js",
    "build:check": "tsc --noEmit",
    "lint": "publint",
    "lint:types": "attw --pack . --ignore-rules no-resolution cjs-resolves-to-esm",
    "prepack": "npm run build",
    "clean": "rm -rf dist wasm-pkg"
  },
  "devDependencies": {
    "tsdown": "^0.12.0",
    "typescript": "^5.8.0",
    "publint": "^0.3.0",
    "@arethetypeswrong/cli": "^0.18.0"
  },
  "keywords": [
    "vector-database",
    "hnsw",
    "wasm",
    "webassembly",
    "rag",
    "embeddings",
    "transformers.js",
    "browser",
    "offline",
    "ai-agent",
    "opfs",
    "web-worker"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/philsupertramp/vecdb-wasm.git"
  },
  "homepage": "https://huggingface.co/spaces/philipp-zettl/vecdb-wasm",
  "sideEffects": false
}
